# =============================================================
# ConfigMap Reference — Terraform Outputs
# =============================================================
# This references the ConfigMap created by sync-terraform-outputs script
# The ConfigMap must exist before ArgoCD can sync this overlay
# =============================================================

apiVersion: v1
kind: ConfigMap
metadata:
  name: terraform-outputs-dev
  namespace: workloads
  labels:
    app: zero-trust
    environment: dev
    managed-by: terraform
# Data is populated by scripts/sync-terraform-outputs.ps1 or .sh
# ArgoCD will use this as a source for Kustomize replacements
data:
  ACR_LOGIN: "acrztdeve85d83.azurecr.io"
  KV_NAME: "kv-zt-dev-e85d83"
  CLIENT_ID: "REPLACE_WITH_YOUR_CLIENT_ID"
  TENANT_ID: "REPLACE_WITH_YOUR_TENANT_ID"
  ENVIRONMENT: "dev"
